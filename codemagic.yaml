workflows:
  android-workflow:
    name: Android Build
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Generate Android project & Build APK
        script: |
          flutter create . --platforms=android
          flutter pub get
          flutter build apk --release --enable-experiment=dot-shorthands
